name: test

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

  workflow_dispatch:

jobs:
  test: 
    runs-on: ubuntu-latest 
    steps:
    - name: Run pwd
      run: pwd
    - name: Run ls -al
      run: ls -al
