spring:
  profiles:
    active: local
---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/live_commerce/serverTimeZone=UTC&CharacterEncoding=UTF-8
    username: root
    password: password

  config:
    activate:
      on-profile: local


mybatis:
  type-aliases-package: com.flab.demo.dto, com.flab.demo.domain
  mapper-locations: classpath:mapper/**.xml

  configuration:
    map-underscore-to-camel-case: true

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/live_commerce/serverTimeZone=UTC&CharacterEncoding=UTF-8
    username: root
    password: password

  config:
    activate:
      on-profile: local


mybatis:
  type-aliases-package: com.flab.demo.dto, com.flab.demo.domain
  mapper-locations: classpath:mapper/**.xml

  configuration:
    map-underscore-to-camel-case: true

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/live_commerce/serverTimeZone=UTC&CharacterEncoding=UTF-8
    username: root
    password: password

  config:
    activate:
      on-profile: development


mybatis:
  type-aliases-package: com.flab.demo.dto, com.flab.demo.domain
  mapper-locations: classpath:mapper/**.xml

  configuration:
    map-underscore-to-camel-case: true

---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/live_commerce/serverTimeZone=UTC&CharacterEncoding=UTF-8
    username: root
    password: password

  config:
    activate:
      on-profile: staging


mybatis:
  type-aliases-package: com.flab.demo.dto, com.flab.demo.domain
  mapper-locations: classpath:mapper/**.xml

  configuration:
    map-underscore-to-camel-case: true


#TODO : 운영 환경에 대한 설정이 정해지면 수정 필요
---
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/live_commerce/serverTimeZone=UTC&CharacterEncoding=UTF-8
    username: root
    password: password

  config:
    activate:
      on-profile: production

mybatis:
  type-aliases-package: com.flab.demo.dto, com.flab.demo.domain
  mapper-locations: classpath:mapper/**.xml

  configuration:
    map-underscore-to-camel-case: true
